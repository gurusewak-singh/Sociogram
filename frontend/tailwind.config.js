module.exports = {
  darkMode: 'class', // Enable Tailwind CSS dark mode support via class strategy
  content: [
    // Paths to all of the template files in your project
    './src/**/*.{js,jsx,ts,tsx}',
    './public/index.html',
  ],
  theme: {
    extend: {
      // Extend the default theme here
    },
  },
  plugins: [
    // Add your plugins here
  ],
}